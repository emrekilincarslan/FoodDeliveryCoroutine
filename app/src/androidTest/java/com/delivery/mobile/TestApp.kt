package com.delivery.mobile

import android.app.Application

/**
 * App for tests to prevent initializing dependency injection.
 *
 * See [com.delivery.mobile.util.AppTestRunner].
 */
class TestApp : Application()
